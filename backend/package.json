{
  "name": "backend",
  "version": "1.0.0",
  "description": "This script builds a video where each image is shown while the corresponding text is spoken. Uses MoviePy and a TTS engine (gTTS or pyttsx3).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "sqlite3": "^5.1.7"
  }
}
